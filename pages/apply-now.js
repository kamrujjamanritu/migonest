import Layout from "../components/Layout/Layout";

const Apply = () => {
    return (
        <div>
            <Layout PageMeta={{
                title: "Migonest Apply Now - Global University Admission Guideline"
            }}>
                <h1>Apply</h1>
            </Layout>
        </div>
    );
}

export default Apply;
