import Layout from "../components/Layout/Layout";

const Settings = () => {
    return (
        <div>
            <Layout PageMeta={{
                title: "Migonest Settings - Global University Admission Guideline"
            }}>
                <h1>Settings</h1>
            </Layout>
        </div>
    );
}

export default Settings;
